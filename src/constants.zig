// All constants' units are raylib pixels or pixels per frame unless otherwise noted.
// See game object definitions for instance-level widths, heights, speeds.
// The following constants define the game set-up, so I kept them top-level.

pub const screenWidth = 800;
pub const screenWidthBy2 = screenWidth / 2;
pub const screenHeight = 600;
pub const screenHeightBy2 = screenHeight / 2;
pub const maxPlayerBullets = 10;
pub const maxInvaderBullets = 20;
pub const invaderRows = 5;
pub const invaderCols = 11;
pub const invaderStartX = 100;
pub const invaderStartY = 50;
pub const invaderSpacingX = 60;
pub const invaderSpacingY = 40;
pub const shieldStartCount = 4;
pub const shieldStartX = 150;
pub const shieldSpacingX = 150;
pub const shieldStartY = 450;
